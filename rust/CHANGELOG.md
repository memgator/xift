# Rust workspace — Change Log

Consolidated change history for the `rust/` workspace crates (`xift-core`,
`xift-client`, `xift-server`, `xift-custodian`, adapters). **Newest first.**

- One row per substantive change. Note the crate(s) affected and the version
  when a crate is released. To record a change: add a row at the top.
- Authoritative history is git (`git log -- rust/`); this file is the curated
  digest. Per-crate `Cargo.toml` versions remain the source of truth for
  published versions.

| Date       | Crate(s) | Notes |
|------------|----------|-------|
| _No entries yet._ | | The reference implementation is in early scaffolding; the first substantive change adds the first row here. |
