# Contracts — Change Log

Consolidated change history for the JSON Schemas and contract documents in
`contracts/` (and `contracts/schemas/`). **Newest first.**

- One row per schema/contract revision. The authoritative history is git
  (`git log --follow -- contracts/<file>`); this file is the curated digest.
- Schemas carry their version in `$id` (`/schemas/v1.0/…`); a backward-
  incompatible change MUST bump that path.

| Date       | Schema / Contract                                | Notes |
|------------|--------------------------------------------------|-------|
| 2026-06-01 | schemas/capability-advertisement.schema.json     | **New** — Channel 1 capability advertisement (§3 baseline + §4 extended as a strict superset). Required baseline (`did`, `name`, `version`, `protocol_version`=const "1.0", `endpoints` closed map, `supported_extensions` enum, `supported_did_methods`, `governance_constraints`, `identity_handshake`, lifecycle, `capability_signature`/`signing_key_id`); optional extended (`channel_capabilities`, `capability_vector` object-or-array, billing-aware `resource_costs`, `custodian_eligible`, `ontology` Mechanism-A per ADR-XIFT-ONTOLOGY-001, `mcp_servers`). `additionalProperties:false`; `$defs` for DID/date-time/trust-score [0,1000]/classification/PII/memory-scope/VCV. Validated draft 2020-12 with the extension registry. |
| 2026-06-01 | schemas/handshake.schema.json                    | **New** — Channel 1 two-phase identity handshake (§7). Four message shapes in `$defs` (`handshake_init_request`/`_response`, `handshake_complete_request`/`_response`) referenced individually by the OpenAPI; root `oneOf` for standalone validation. XIFT-defined fields closed (`additionalProperties:false`); method-specific Phase-B payloads (`initiator_proof`, `responder_proof`, `trust_score_assertion`) carried in an open `method_specific` `$def`. `responder_trust_score` bounded [0,1000] (no 700 gate in P2P Flow A). |
| 2026-06-01 | schemas/xift-semantic-query.schema.json          | **New** — Channel 5 `XiftSemanticQuery` (§3, §4). Signed query with `constraints` (trust/classification/PII/purpose/scopes/`max_results`≤50/latency/budget + optional string-typed `max_cost_per_result`), `scope_redaction_applied` egress assertion, and OPTIONAL `ontology` descriptor (`$ref` to `extension-ontology.schema.json`, §7.1). `dependentRequired`: `query_embedding`⇒`embedding_model`. 4 KB `sdr_query_size_max` is protocol-enforced, not in schema. |
| 2026-06-01 | schemas/xift-semantic-response.schema.json       | **New** — Channel 5 `XiftSemanticResponse` bundle (§5–§7). `matches[]` of `Match` (`$def`: `candidate_did`/`composite_score`/`handoff_endpoint` required; optional `envelope_id_hint`, four-dimensional `score_breakdown`, `preview`≤200, governance attrs, `scs_endpoint`). Scores in [0,1] `unit_interval`. `if/then`: `completed:false`⇒`partial_reason` required. |
| 2026-06-01 | openapi/xift-api.yaml                             | **Authored** (was empty) — OpenAPI 3.1.0 synchronous API, Flow A subset. Channel 1 (`PUT`/`GET /discovery/self`, `GET /discovery/{did}`, `POST /handshake/init`+`/complete`) and Channel 5 (`POST /sdr`), `$ref`-ing the schemas above (including the handshake `$defs`). Per-agent templated server URL; `signatureAuth`/`bearerAuth` security schemes; inline `XiftError` component per core §12.2 / ADR-XIFT-ERROR-MODEL-001 with error responses citing registered categories. Channel 2 handoff + async (4/6) deferred. |
| 2026-06-01 | schemas/memory-object.json                       | **Authored** the KnowledgeObject envelope schema (was an empty stub). Flat top-level mandatory blocks per core §3.3 (Identity/Operational/Content/Security) with frozen wire names (`envelope_id`, `memory_scope`, `payload_inline`/`content_ref`, `payload_hash`, `content_type`, …); `oneOf` enforces exactly one of `payload_inline`/`content_ref` (and `payload_hash` required with `content_ref`); `additionalProperties:false` with the six extension blocks as `$ref`s to their `$id` (`extension-*.schema.json`); `dependentSchemas` encodes revocation⇒governance (core §3.3.5). `$defs` for ULID/DID/`sha256:` hash/date-time. Validated against draft 2020-12 with the extension registry. |
| 2026-06-01 | schemas/extensions/governance.schema.json        | Added the optional `native_labels` property (ADR-XIFT-GOVERNANCE-LABELS-001, governance §3.10): an array of scheme-scoped records (`scheme`, optional `scheme_version`/`authority`/`tenant_ref`, `labels[]`), each label requiring a stable `id` with optional `name`/`parents`/`score`/`attributes`. `attributes` is the only `additionalProperties:true` object (verbatim source metadata); all other objects stay closed. Size/count bounds (`native_labels_count_max`, `native_labels_bytes_max`) are protocol-enforced, not expressed as `maxItems`. Backward-compatible (optional field); `$id` version unchanged. |
| 2026-05-31 | schemas/extensions/ontology.schema.json          | **New** — schema for the sixth extension `ontology` (ADR-XIFT-ONTOLOGY-001). Validates the silently-ignorable vocabulary **descriptor** block (`context_iri`, `context_hash` `sha256:` pin, `format` enum, optional reduced `skos_projection` bounded at 256 concepts, optional `shacl_shapes`) and provides `$defs/alignment_cell` (SSSOM-inspired) for reuse by the Channel-7 SCS synopsis. Mirrors the `quality` schema conventions (`additionalProperties:false`, `$defs`, JCS float note). Code-enforced invariants flagged via `x-xift-note` (`score_function` ↔ `method` consistency; projection privacy). |
